extends: [["spectral:oas", "recommended"], "../ruleset.json"]
functionsDir: "../functions"
functions: ["abc"]
rules:
  require-test-policy:
    severity: error
    given: "$"
    then:
      function: "abc"
